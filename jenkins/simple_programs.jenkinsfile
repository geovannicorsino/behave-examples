pipeline {
    agent { dockerfile true }
       stage('Tests') {
           steps {
               {
                    sh 'behave src/simple_programs/features'
               }
           }
       }

    }
    post {
        always{
            junit 'TESTS*.xml'
        }
    }